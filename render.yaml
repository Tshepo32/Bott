services:
  - type: web
    name: resume-chatbot
    env: python
    runtime: python3
    plan: free

    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT

    healthCheckPath: /
    numInstances: 1
